FROM node:18-alpine

WORKDIR /app

# Copiar package.json y package-lock.json
COPY package*.json ./

# Instalar dependencias
RUN npm install

# Copiar el código del servidor
COPY server/ ./server/

# Exponer el puerto
EXPOSE 4000

# Comando por defecto
CMD ["npm", "run", "dev"]
